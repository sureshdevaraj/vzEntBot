Enter file contents herefunction recommendTVNew(callback) { 
     
    request( 
         'http://www.verizon.com/msvsearch/whatshot/tv?op=now&region=91812&lrg=1', 
         function (error, response, body) { console.log('inside external call');
             if (!error && response.statusCode == 200) { 
                console.log('inside external call success');
                console.log(body);
                 callback(body); 
             } 
             else           console.log('error: ' + error + 'response:' + response.statusCode + 'body:' + body);
         } 
     ); 
  }
  
  https://example-code.com/nodejs/create_complex_json.asp
  Node.js 6.*.*, 64-bit Windows: npm install chilkat_node6_win32
  
  http://codesamplez.com/programming/using-json-in-node-js-javascript
